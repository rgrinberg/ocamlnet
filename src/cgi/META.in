requires = "netstring"
version = "@VERSION@"
description = "Ocamlnet - Common Gateway Interface"
archive(byte) = "cgi.cma"
archive(native) = "cgi.cmxa"
error(pkg_netcgi) = "The packages cgi and netcgi cannot be used together"
